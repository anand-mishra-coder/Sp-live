import {db,collection,addDoc,serverTimestamp} from "./firebase.js";export async function sendNotification(userId,title,message,type="general"){return addDoc(collection(db,"notifications"),{userId,title,message,type,read:false,createdAt:serverTimestamp()})}export async function broadcastAnnouncement(title,message){return addDoc(collection(db,"announcements"),{title,message,priority:"normal",createdAt:serverTimestamp()})}
